try:
    with open('text.txt', 'r', encoding='utf8') as file_name:
        for i_name in file_name:
            print(i_name.count('\n'))
            if len(i_name) < 3 + i_name.count('\n'):
                raise TypeError('Ля ля тополя')
except TypeError:
    print('123456789')
